{
  "name": "capyplan-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:web": "pnpm --filter web dev",
    "dev:signal": "pnpm --filter signal dev",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "typescript": "^5.3.0"
  }
}